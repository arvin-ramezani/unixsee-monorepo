/**
 * @deprecated Step 8 removed all machine-id compatibility. Import from
 * `identity.ts` in new code. This re-export exists only to avoid stale imports
 * during the step-by-step migration.
 */
export { resolveAgentInstanceId as resolveAgentInstanceIdCompat } from "./identity.js";
